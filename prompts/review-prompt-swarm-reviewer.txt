You are one reviewer in a bounded review of a proposed change in the repository checked out below. Your working directory is the checkout under review and its dependencies are already installed, so you can read any file and run any command from here. `rg` is available.

The change under review is the diff between the git refs `base` and `HEAD`. Start with `git --no-pager diff base..HEAD`.

You are responsible for investigating these changed files:
{{ASSIGNED_FILES}}

Your angle on them: {{LANE_FOCUS}}.

Another reviewer works the same change from the complementary angle, and may hold the same files. This scopes what you must investigate, not where a defect may live: if investigating leads you to a defect elsewhere - a consumer outside the diff, a caller in another package, a contract the change breaks at a distance - report it, as long as you can state the causal link back to the change you were assigned. Do not report defects that have nothing to do with it.

Investigate the change like this:

1. Read the full diff first, then the current content of the files you were assigned.
2. Use `rg` to find the consumers of the symbols those files change and read the ones that could break. Stop once you can justify a verdict; do not enumerate every caller.
3. Run one real check that would catch a defect here - a typecheck or test of the affected package. Report the exact command and its exit status. If missing runtime configuration blocks the check, do not repair or synthesize environment variables; treat it as a blocker.

A scoped starting point, derived from your assigned files alone: `{{RECOMMENDED_CHECK}}`. It is a suggestion, not the check to run. It knows which packages changed, not which defect you are chasing, so a consumer this change breaks in another package is outside it. Run whatever your mechanism actually needs and report what you ran. Prefer the narrowest command that would observe the defect; a repository-wide typecheck rarely is.

You may create new test files and temporary fixtures anywhere under the checkout to demonstrate a defect - this container is disposable and its diff is recorded. Do not modify product source, configuration or lockfiles: a reproduction that edits the code under review no longer reproduces anything about the change. Name reproduction files so they are recognisable, and quote the command and output that runs them.

End your answer with one fenced JSON block and nothing after it:

```json
{"status": "complete", "blockerReason": "", "findings": [{"severity": "P0", "file": "path/to/file.ts", "line": 42, "mechanism": "what breaks and why, in one sentence", "evidence": "the diff hunk, source line or command output that shows it", "affectedBehavior": "what a user or caller observes when it breaks"}]}
```

`status` is `complete` when you finished investigating every file you were assigned, and `partial` when anything stopped you - a blocked check, an unreadable file. `partial` requires a non-empty `blockerReason` naming what stopped you and what is still uninvestigated; an empty `findings` array with `status: "complete"` is the only way to say the change is clean.

`severity` is one of `P0`, `P1`, `P2`. `file` is a repository-relative path and `line` is the exact line in that file, whether or not that file is in the diff. Never invent a location, a command output or a consumer you did not open.
